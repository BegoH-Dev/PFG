# Node.js : ignora la carpeta node_modules y todo lo que contiene. 
# El "*" indica que ignora todos los archivos que terminen en ".log" de errores de npm
node_modules/
npm-debug.log*

# Archivos de compilación : salida de compilación y carpeta de compilación de React
dist/
build/

# Registros (logs) : el "*" indica que ignora todos los archivos que terminen en ".log" de errores de npm
logs/
*.log

# Archivos de entorno (credenciales, claves, etc.) : el "*" indica que ignora todos los archivos que terminen en ".env"
.env
.env.*

# Archivos específicos del sistema operativo : 
# "Thumbs.db" es para Windows, pero ".DS_Store" de macOS lo he dejado por si alguien que usa macOS quiere ver el proyecto 
# sin problemas de compatibilidad y pueda visualizar el proyecto bien
.DS_Store
Thumbs.db

# Archivos del editor o IDE : configuraciones de VS Code, JetBrains (WebStorm, etc.) y archivos temporales de editores
# El "*" indica que ignora todos los archivos que terminen en ".swp"
.vscode/
.idea/
*.swp

# Docker : configuración específica local de Docker. También evita subir archivos docker si son locales o temporales
docker-compose.override.yml
.dockerignore
Dockerfile

# Git : repositorio Git en sí mismo
.git/

# Volúmenes de PostgreSQL (datos persistentes) : ignora todos los archivos ".sql", como los backups de la base de datos
pgdata/
*.sql

# Específico de frontend y backend
frontend/node_modules/
frontend/build/
frontend/dist/

backend/node_modules/
backend/build/
backend/dist/
